# ActiveAdmin.register Benefit do
#   permit_params :category_id, :name, :allowed_values, :allowed_values_count

#   menu priority: 10
#   filter :category_id, label: 'Category', as: :select, collection: Category.all.map { |category| [category.id, category.name] }

#   actions :all
# end
